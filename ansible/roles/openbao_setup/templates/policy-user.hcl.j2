# User policy for {{ user_item.name }}

# Access to own namespace secrets
path "{{ user_item.name }}/*" {
  capabilities = ["create", "read", "update", "delete", "list", "sudo"]
}

# Access to shared namespace
path "shared/*" {
  capabilities = ["create", "read", "update", "delete", "list", "sudo"]
}

# Allow OIDC authorization flow (required for Gitea/Grafana/ArgoCD SSO)
path "identity/oidc/provider/+/authorize" {
  capabilities = ["read"]
}

# Allow token self-lookup and renew (required for OIDC session)
path "auth/token/lookup-self" {
  capabilities = ["read"]
}

path "auth/token/renew-self" {
  capabilities = ["update"]
}
