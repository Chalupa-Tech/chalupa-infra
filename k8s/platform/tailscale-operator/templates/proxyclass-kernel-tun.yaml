apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: kernel-tun
spec:
  statefulSet:
    pod:
      tailscaleContainer:
        env:
          - name: TS_USERSPACE
            value: "false"
        securityContext:
          privileged: true
