apiVersion: v1
kind: Service
metadata:
  name: go-notify-subscriber
  labels:
    app: go-notify-subscriber
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: go-notify-subscriber
