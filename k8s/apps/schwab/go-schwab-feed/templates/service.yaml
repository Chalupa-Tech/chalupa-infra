apiVersion: v1
kind: Service
metadata:
  name: go-schwab-feed
  labels:
    app: go-schwab-feed
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: go-schwab-feed
