apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-schwab-feed
  namespace: observability
  labels:
    grafana_dashboard: "1"
data:
  schwab-feed.json: |
{{ .Files.Get "files/schwab-feed.json" | indent 4 }}
