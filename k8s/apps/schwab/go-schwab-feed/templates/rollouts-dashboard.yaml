apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-schwab-rollouts
  namespace: observability
  labels:
    grafana_dashboard: "1"
data:
  schwab-rollouts.json: |
{{ .Files.Get "files/schwab-rollouts.json" | indent 4 }}
