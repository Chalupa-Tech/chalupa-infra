apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: go-schwab-auth
  labels:
    app: go-schwab-auth
spec:
  selector:
    matchLabels:
      app: go-schwab-auth
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
